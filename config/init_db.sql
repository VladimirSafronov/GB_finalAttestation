USE humanFriends;

CREATE TABLE cat (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);

CREATE TABLE dog (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);

CREATE TABLE hamster (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);

CREATE TABLE horse (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);

CREATE TABLE camel (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);

CREATE TABLE donkey (
	id INT PRIMARY KEY AUTO_INCREMENT,
	name CHAR(32),
    age DATE,
    commands TEXT
);